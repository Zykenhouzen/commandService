package main

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestGroupSplit(t *testing.T) {
	//This assumes main.go is refactored to have a function that takes a string and returns groups [][]string
	//sampleInput := "2:1 a b c"
	//grouping := SplitGroup(sampleInput)
	//assert.Equal(t, 2, len(grouping))
	// Add other interesting checks here.

	assert.True(t, true)
}
